<div id="forecast"
  data-checkpoints="<%= trip.checkpoints.first(trip.checkpoints.count-1).to_json %>"
  data-trip-dates="<%= trip_dates.first(trip_dates.count-1).to_json %>"
  data-client-id="<%= ENV['AERIS_CLIENT_ID'] %>"
  data-client-secret="<%= ENV['AERIS_CLIENT_SECRET'] %>">
</div>